# Backend + 코어(`app`) 실행에 필요한 의존성 (pyproject.toml 과 동기화)
# 저장소 루트에서:
#   pip install -e .
# 또는 이 파일만으로 평면 설치:
#   pip install -r requirements/backend.txt

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1,<5
pydantic[email]>=2.6.0
pydantic-settings>=2.2.1
python-dotenv>=1.0.0
httpx>=0.27.0
requests>=2.31.0
cryptography>=42.0.0
sqlalchemy>=2.0.0
pandas>=2.2.0
redis>=5.0.0
