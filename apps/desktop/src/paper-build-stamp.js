// Paper 화면용 UI 빌드 스탬프. Windows 설치용 빌드(`npm run build:win`) 시 잠시 git SHA로 덮어쓴 뒤 복원됩니다.
window.PAPER_UI_BUILD_STAMP = {
  stamped_utc: "",
  git_sha: "(dev workspace — not installer build)",
  npm_pkg: "0.1.0",
};
