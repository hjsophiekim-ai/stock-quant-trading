{
  "name": "stock-quant-mobile",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "start": "expo start --clear",
    "dev": "expo start --clear",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "prebuild": "expo prebuild",
    "build:android:apk": "npx eas build --platform android --profile preview-apk",
    "build:android:aab": "npx eas build --platform android --profile production",
    "build:ios:preview": "npx eas build --platform ios --profile preview-apk",
    "build:ios:prod": "npx eas build --platform ios --profile production"
  },
  "dependencies": {
    "@react-native-picker/picker": "2.7.5",
    "@react-native-async-storage/async-storage": "1.23.1",
    "base-64": "1.0.0",
    "expo": "^51.0.0",
    "react": "^18.2.0",
    "react-native": "^0.74.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
